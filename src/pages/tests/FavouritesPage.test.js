import React from 'react';
import { shallow } from 'enzyme';
import FavouritesPage from '../FavouritesPage';

describe('pages/FavouritesPage', () => {
  it('should render properly', () => {
    expect(FavouritesPage).toBeDefined();
  });
});
